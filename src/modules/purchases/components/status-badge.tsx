/**
 * Purchase status badges now live in the shared component, because customer
 * invoices share the same lifecycle as vendor bills.
 */
export { BillStatusBadge, OrderStatusBadge } from "@/modules/shared/components/status-badge";
